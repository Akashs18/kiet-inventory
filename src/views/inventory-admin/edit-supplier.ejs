<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Supplier</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f4f6f8;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #333;
    }

    .card {
      background: #fff;
      width: 100%;
      max-width: 420px;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.08);
    }

    h2 {
      color: #5563DE;
      margin-bottom: 20px;
      text-align: center;
    }

    .error {
      background: #ffe6e6;
      color: #b30000;
      padding: 10px 14px;
      border-radius: 8px;
      margin-bottom: 15px;
      font-size: 14px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }

    input {
      padding: 12px 14px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 15px;
    }

    input:focus {
      outline: none;
      border-color: #5563DE;
      box-shadow: 0 0 0 2px rgba(85, 99, 222, 0.15);
    }

    button {
      margin-top: 10px;
      padding: 12px;
      border: none;
      border-radius: 8px;
      background: #5563DE;
      color: white;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: #3b46b1;
    }

    .back {
      display: block;
      margin-top: 18px;
      text-align: center;
      text-decoration: none;
      font-weight: bold;
      color: #555;
    }

    .back:hover {
      color: #000;
    }
  </style>
</head>

<body>

  <div class="card">
    <h2>Edit Supplier</h2>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/inventory-admin/suppliers/edit/<%= supplier.id %>">
      <input
        type="text"
        name="name"
        placeholder="Supplier Name"
        value="<%= supplier.name %>"
        required
      />

      <input
        type="text"
        name="contact"
        placeholder="Contact"
        value="<%= supplier.contact %>"
        required
      />

      <input
        type="text"
        name="address"
        placeholder="Address"
        value="<%= supplier.address %>"
        required
      />

      <button type="submit">Update Supplier</button>
    </form>

    <a href="/inventory-admin/suppliers" class="back">← Back to Suppliers</a>
  </div>

</body>
</html>
